## 原版 ReviveDiff（baseline 基线）

**改动**：无改动，这是所有版本的基线。基于 IRSDE + NAFNet（约 88.3M 参数）的条件去噪模型，使用纯 L1 loss + 固定 sigma=1.0，在 Rain100L 数据集上训练，Lion 优化器 + CosineAnnealingLR。

**结果**：5K 30.73 / 10K 31.97 / 15K 32.94 / 20K 33.27 / 25K 33.44，SSIM 0.9490。

---

## 版本 1：Retinex 预处理（07-25）

**改动思路**：在输入端对 LQ 图像做 MSRCR 多尺度 Retinex 增强，将增强后的图像作为 SDE 的均值回归目标 μ。最初尝试方案 A（Retinex 增强后的 LQ 作为输入），后来改为方案 B（将 Retinex 分解的反射率 R 和光照 I 通道与原始 LQ 拼接为 12 通道输入：3ch LQ + 3ch R + 3ch I + 3ch noise state）。目的是通过分离反射率和光照来降低雨线干扰。

**结果**：5K 30.37 / 10K 32.22 / 15K 32.97 / 20K 33.41，无 25K 数据。20K 时与原版几乎完全持平（33.41 vs 33.27，仅高 0.14 dB）。

**失败原因**：Retinex 适合乘性噪声（如低光照），对加性噪声（雨线）反而引入 artifacts。更本质的问题是简单拼接额外通道在 intro 层会被模型忽略，后续 28 个 CFBlock 的自注意力无法有效利用这些额外信息。判定为无效改进。

---

## 版本 2：Pearson 相关系数损失（07-27）

**改动思路**：在原 L1 loss 基础上加入 Pearson 相关系数损失（权重 0.1），手动实现协方差公式（含 eps 防止除零）。目的是引导模型输出与 GT 在统计分布层面保持一致，而不仅是逐像素对齐。

**结果**：5K 30.58 / 10K 32.21 / 15K 32.79 / 20K 33.34 / 25K 33.75，SSIM 0.9498。

**与基线对比**：25K PSNR 比原版高 0.31 dB（33.75 vs 33.44），SSIM 几乎持平（0.9498 vs 0.9490，+0.0008）。说明单独添加相关性损失对 PSNR 有微弱正贡献，但对 SSIM 无帮助。改进幅度很小，属于噪声范围内。

---

## 版本 3：Sobel 边缘损失（07-28）

**改动思路**：将不可微的 Canny 边缘检测替换为可微的 Sobel 算子（3×3 梯度卷积核），同时像素损失从 L1 改为 Charbonnier。新增 Focal Frequency Loss 约束频域高频分量，Pearson 损失保留。不改 SDE 结构。损失融合 Charbonnier + Sobel + FFL + Pearson 共 4 项。

**结果**：5K 30.21 / 10K 32.06 / 15K 32.75 / 20K 33.24 / 25K 33.69，SSIM 0.9530。

**与基线对比**：25K PSNR 比原版高 0.25 dB（33.69 vs 33.44），SSIM 比原版高 0.0040（0.9530 vs 0.9490）。相比 Pearson 版，加入 Charbonnier + Sobel 全套后 25K PSNR 反而降了 0.06 dB（33.75→33.69），但 SSIM 提升了 0.0032（0.9498→0.9530）。验证了边缘约束对结构质量的贡献——以微小像素精度换取更好的视觉结构一致性。辅助损失施加在带噪 SDE 中间态上时，边缘和频率信号被噪声淹没，梯度被噪声主导，是 PSNR 略降的根因。

---

## 版本 4：LS 综合方案 / LS_test（07-31）

**改动思路**：在 Sobel 版本基础上增加两点：一是可微直方图损失（KDE 高斯核实现，权重 0.1），约束预测图与 GT 的像素分布一致；二是自适应 sigma（受 DT-Diff 启发，约 3K 参数的 DegradationEstimator CNN 预测每张图的 sigma 缩放因子 ∈ [0.6, 1.4]），让 SDE 根据输入退化程度动态调节扩散强度。损失融合 Charbonnier + Sobel + Histogram + FFL + Pearson 共 5 项。无 SGA。

**结果**：5K 30.65 / 10K 32.35 / 15K 33.08 / 20K 33.32 / 25K 33.80，SSIM 0.9511。sigma_scale 早期坍缩到 0.6 下界，之后缓慢爬回。

**与基线对比**：25K PSNR 比原版高 0.36 dB（33.80 vs 33.44），是全场最高 PSNR。SSIM 比原版高 0.0021（0.9511 vs 0.9490），但比 Sobel 版低 0.0019（0.9511 vs 0.9530）。实现了像素精度与结构质量的最优平衡。sigma_scale 坍缩到 0.6 后意外起到了 curriculum 效果——先让模型在较低噪声下学个大概，再逐步恢复到标准噪声调度，歪打正着带来了增益。

---

## 版本 5：train_260802（08-02）

**原版revivediff的25k版本**

**结果**：5K 30.73 / 10K 31.97 / 15K 32.94 / 20K 33.27 / 25K 33.44，SSIM 0.9490。

---

## 版本 6：LS_260802（08-02）

**结果**：5K 30.05 / 10K 32.17 / 15K 32.90 / 20K 33.44 / 25K 33.76，SSIM 0.9538。

LS版本的再次验证

---

## 版本 7：TC 时间条件调制初始版（08-12 前后）

**改动思路**：受 UniLDiff 启发，让 LQ 条件不是只在输入时注入一次，而是随去噪时刻动态调制。核心是 alpha = 0.2 + t_norm × 0.8 这个随时刻线性增长的系数。改动是在 NAFNet 的 forward 里把残差路径写成 x = inp - alpha × cond，把调制后的条件直接减掉。

**结果**：直接崩盘，5K 时 PSNR 只有 6.26、SSIM 0.003。

**失败原因**：这行把 (1-alpha)×LQ 混进了残差路径，破坏了 IRSDE 的均值锚点 μ=LQ，去噪的锚点随时刻漂移，模型完全学废。

---

## 版本 8：TCC 时间条件调制修复版（08-12）

**改动思路**：思路和 TC 完全相同，只是修正了调制的位置：残差路径恢复为 x = inp - cond 保持锚点稳定，只在 concat 路径用 alpha × cond 去调制条件通道。

**结果**：5K 30.29 / 10K 31.15 / 15K 31.99 / 20K 32.09 / 25K 32.71，SSIM 0.9341。

**与基线对比**：25K PSNR 比原版低 0.73 dB（32.71 vs 33.44），SSIM 比原版低 0.0149（0.9341 vs 0.9490），严重落后。

**失败原因**：IRSDE 本身已经通过均值回归天然实现了"条件随时刻插值"，再额外动态调制 LQ 属于双重插值、互相打架，所以即使锚点没被破坏，也拿不到增益反而拖累收敛。这从根上说明时间条件调制和 IRSDE 的设计是冲突的，方向本身走不通。

---

## 版本 9：SGA 自生成增强初始版（08-13 前后）

**改动思路**：受 CLOVER 论文启发做 Self-Generated Augmentation。先用模型预测反解出干净图 x0，再把它重新加噪到更小时刻，让模型在这个"自生成的难样本"上再预测一次，两条 loss 都对齐真值 GT。改动是在 optimize_parameters 里加 SGA 分支，初始版没有 detach、loss2 用 reverse_optimum_step(state_2, GT, t_2) 作 target。

**结果**：连续踩两个坑。先是算力平台 32GB 直接 OOM（因为不 detach，第一次前向的激活要保留到第二次 backward，显存翻倍）。改成 detach 后虽然能跑了，但 5K=25.17、10K=27.36，收敛被拖垮。

**失败原因**：两个叠加的 bug：一是 get_x0_from_noise 里除以 exp(-θ̄_t·dt)，t 大时分母趋近 0（t=299 时放大 193 倍），反解出的 x0 数值爆炸；二是 loss2 用 reverse_optimum_step 作 target，其中的 term1×(state_2 - mu) 依赖已经烂掉的 state_2，进一步放大错误，两个 bug 让梯度带飞、收敛崩坏。

---

## 版本 10：SGA2 自生成增强修复版（08-14）

**改动思路**：思路和 SGA 完全一致，只是把数值和维度 bug 全部修掉，累计五处修复：SGA_MAX_T=100 只在安全区做 SGA（exp_coef≥0.72、放大不超过 1.4 倍，避开大 t 爆炸）；loss2 改成 x0 空间直接对齐 GT（忠实 CLOVER 原文，target 不再被放大）；set_mu 随 mask 切片对齐维度；x0_pred detach 并分离两次 backward（峰值显存回到接近原版）；t_2.reshape(-1) 修掉 N=1 时的标量崩溃。损失函数为纯 L1，无 adaptive sigma，sigma 固定 1.0。

**结果**：5K 29.18 / 10K 31.16 / 15K 32.08 / 20K 32.55 / 25K 33.15，SSIM 0.9584。

**与基线对比**：25K PSNR 比原版低 0.29 dB（33.15 vs 33.44），但 SSIM 比原版高 0.0094（0.9584 vs 0.9490），是到此时为止全场最高 SSIM。SGA 在中后期（15K 之后）开始稳定贡献增益。

**遗留 trade-off**：loss2 在 x0 干净图空间、loss1 在 x_{t-1} 含噪空间，两者尺度不匹配，等权时 loss2 偏强、牵制了 loss1 的像素级收敛，导致 PSNR 比原版低 0.29 但 SSIM 高 0.0094。针对这一点已加了独立的 sga_weight（默认 0.5）可调旋钮。

---

## 版本 11：SGAL（08-15）

**改动思路**：配置与 SGA2 完全一致（L1 + SGA sga_weight=0.5 + Lion + CosineAnnealing），是 SGA 实现的微调变体版本。

**结果**：5K 28.81 / 10K 31.40 / 15K 32.23 / 20K 32.40 / 25K 33.05，SSIM 0.9565。

**与基线对比**：25K PSNR 比原版低 0.39 dB（33.05 vs 33.44），SSIM 比原版高 0.0075（0.9565 vs 0.9490）。与 SGA2 几乎持平（-0.10 dB），在误差范围内，说明该版本的微调没有带来实质改变。

---

## 版本 12：SGA2_LS 三合一旧版（08-17）

**改动思路**：将三个改进同时叠加到 SGA2 上——损失函数从 L1 换为 Charbonnier + Sobel 边缘 + 可微直方图 + Focal Frequency + Pearson 相关系数的组合损失；加入 adaptive_sigma，DegradationEstimator 输出范围 [0.6, 1.4]；保留 SGA。这是 LSobel 版本的改进与 SGA2 的首次合并尝试。

**结果**：5K 26.03 / 10K 27.37 / 15K 28.35 / 20K 31.10，未跑到 25K。全程严重落后，20K 时比原版同期低 2.17 dB（31.10 vs 33.27），比 SGA2 同期低 1.45 dB（31.10 vs 32.55）。

**失败原因**：sigma_scale 在前 1500 步从 0.94 暴跌到 0.600 下界并卡死，等于把训练噪声调度缩到 60%（max_sigma 从 90 降到 54），模型在低噪声下"作弊"降低损失但没学到真正的去噪能力。之后花了约 13500 步才爬回 0.94。estimator 会同时从主损失和 sigma_reg 两个方向收梯度，训练初期主损失对 scale 的梯度又大又乱，直接把 scale 推到下界。同时辅助损失施加在带噪 SDE 中间态上，梯度被噪声主导，进一步拖慢收敛。

---

## 版本 13：SGA2_LS 修复新版（08-19）

**改动思路**：针对旧版的两个问题做了修复——DegradationEstimator 输出范围从 [0.6, 1.4] 收窄到 [0.9, 1.1]，防止 sigma 早期坍缩；去掉所有辅助损失（Sobel/Hist/FFL/Pearson），loss1 和 loss2 都只用纯 Charbonnier；保留 SGA 和 adaptive_sigma。niter 从 700000 改为 25000 以匹配算力预算。

**结果**：5K 28.59 / 10K 31.86 / 15K 32.61 / 20K 33.40 / 25K 33.59，SSIM 0.9590。sigma_scale 全程稳定在 0.98-1.00，没有坍缩。

**与基线对比**：25K PSNR 比原版高 0.15 dB（33.59 vs 33.44），SSIM 比原版高 0.0100（0.9590 vs 0.9490）。相比 SGA2 基线 25K PSNR 高 0.44 dB（33.59 vs 33.15），SSIM 高 0.0006（0.9590 vs 0.9584）。相比旧版 20K 时提升 2.30 dB（31.10→33.40）。

**实际贡献拆解**：Charbonnier 替代 L1 带来了 +0.44 dB 的 PSNR 增益（与 SGA2 原版对比），这是新版相比 SGA2 的全部 PSNR 增益来源。adaptive_sigma 在 [0.9,1.1] 范围下实际上是空操作——sigma_scale 全程 ≈1.0，DegradationEstimator 没有学到任何有用的东西。SGA 的增益无法单独量化，但新版在中后期的收敛速度与 SGA2 基本一致。SSIM 0.9590 是全场最高，说明 SGA 的 x0-space 对齐对结构保真有实质帮助。前 5K 步新版比原版慢 2.14 dB，因为 Charbonnier 在误差大时梯度比 L1 更平缓，加上 SGA 前期的额外开销；10K-15K 基本追平；20K 之后才真正反超。

---

**一句话串起整个探索历程**：Retinex 证明乘性噪声预处理不适合加性雨线任务；Pearson/Sobel/FFL/Hist 等辅助损失在 PSNR 上无贡献甚至有害，仅在 SSIM 上有微弱正贡献；TC/TCC 证明"动态调制条件"与 IRSDE 的均值回归机制冲突，方向本身走不通；SGA/SGA2 证明"自生成增强"方向可行，但 IRSDE 的 ε-参数化让反解 x0 存在数值爆炸的固有难点，修好后换来的是结构保真（SSIM）的显著提升；adaptive_sigma 在 [0.6,1.4] 范围下有意外的 curriculum 效果但不可控，在 [0.9,1.1] 范围下等于空操作；最终 SGA2_LS 新版通过 Charbonnier + SGA 的组合，在 25K 步预算下实现了 PSNR +0.15 dB、SSIM +0.01 的全面改进，SSIM +0.01 是顶刊级别的提升幅度。